# 2026-06-03

## 19:49 Self-Driven cron 根治 (cron 5d7486d7)

**问题**：连续 5 次 error（consecutiveErrors=5），自动巡检失效。

**根因分析**（翻了 30+ 历史 runs）：
- 部分失败是 model 行为漂移（5c2dba7f isolated session 0 tool_use = model 假装在跑）
- 部分失败是 minimax provider API 端 cooldown/overloaded（"All models failed", "The AI service is temporarily overloaded"）
- 全部都是 V 端解决不了的间歇性过载

**之前 A+C 修复（19:45 已做）**：
- model 改 M2.7 + 480s timeout + 强 prompt
- 立即 work：19:47 manual ok (86s) + 19:49 scheduled ok (12.9s)

**根治加固（19:49）**：
- schedule 5min → **15min**（减 cooldown 撞车）
- failure-alert: 连续 3 次 error → webchat 推送，cooldown 1h
- state consecutiveErrors: 5 → 0 自动 reset

**新 prompt 关键约束**：
1. 必须用 exec 工具（禁止纯文本推断）
2. 不要 git fetch / push
3. Watt 状态必须 pgrep + 端口监听双检查（仅 pgrep 不算）
4. 每个 exec 限 5s

**Watt Toolkit 实际状态**：
- 进程在（pid 13377/13380）
- **GUI 没启**（Wayland 无窗口、DISPLAY 空）
- **代理端口全关**（26561/44329/7890 都不在 ss 列表）
- = 僵尸状态（启了 .NET runtime 但没登录=没启用代理）

**cron payload 暂不支持的根治项**：
- ❌ fallback model 数组（V cron 工具 patch schema 限制）
- ❌ cooldown 主动跳过（cron 没暴露 hook）

**改进方案**：
- 5min → 15min（已做）
- failure-alert（已做）
- prompt 让 model 检测到 API 错误时立即输出 "skip" 退出（不优雅但够用）

## 17:44 早间 heartbeat 记录

- 静默期，5min cron running
- 4 agent 仓全同步
- VCPToolBox / Ollama / Watt 持续在跑

## 19:44 V design guideline 写入

- 路径：`~/.openclaw/workspace/docs/design-guidelines.md`
- 来源：AgentMemory 2.0 草案抽 4 模式
- ARCHITECTURE 源文件存档：`~/AgentMemory/docs/ARCHITECTURE-2.0-draft.md`

## 20:10 Spring Cleanup（V 主动改进 + V journal）

**触发**：浮光"启动超级大脑，思考改进点，整理成经验发 GitHub"

### 3 件主动改进

1. **workspace 卫生**
   - 17 个 ROCm `.deb` 挪到 `~/下载/roc-debs/`（腾出 2.5GB）
   - 4 个 bench 脚本归档 `tools/bench/`
   - `vm.png`（494KB）删
   - `install_helper.py` → `archive/`
   - 3 个 `/tmp` 临时 bench 脚本 → `archive/`

2. **Watt 启动/状态脚本**（`tools/`）
   - `watt-start.sh`（2.0KB）—— GUI 启，自动找 Xwayland XAUTHORITY
   - `watt-status.sh`（1.3KB）—— 4 态机（DOWN/ZOMBIE/BOOTING/OK）
   - **关键发现**：Watt 走 Avalonia X11 不是 Wayland，GNOME Wayland 下必须 XAUTHORITY
   - **状态机写入 MEMORY.md**（避免下次重新诊断）

3. **V journal 目录 + GitHub 发布**
   - `docs/v-journal/README.md` 建立
   - 首篇 `2026-06-03-spring-cleanup.md`（9KB）
   - 推到 `fuguang8848/Agent-superthinking`（YintaTriss 403）
   - commit `ef326bf`，rebase 解决 ddc830f sync fork commit 冲突

### 5 个 brainstorm 结论

1. workspace 卫生 = "无规则"症状 → 写规则
2. 工具链 4+4+2 = 抽象层没想清楚 → **这次不合并**（避免为改而改）
3. Watt 僵尸 = 知识没沉淀 → 写脚本固定下来
4. V 缺元认知 → journal 公开化让外部审阅
5. 长期挂账 5 项 → 记账不动

### 5 个系统性问题（短期不动 + 触发条件）

1. MEMORY.md 19K 缺章节（触发：>50K）
2. 4+4+2 工具重叠（触发：7 天观察期）
3. 决策日志缺失（触发：复盘错误决策时）
4. 踩坑 wiki 缺失（这次 watt-start.sh 是副产品）
5. 元认知缺位（鸡生蛋，等 event-tracker 跑 1 个月）

### 推到 GitHub 教训

- YintaTriss 是别人仓，浮光没写权限（403）
- 推自己 fork + 必要时 PR 是 GitHub 标准流程
- 推前 rebase 远端领先 commit（ddc830f sync fork）
- 不主动开 PR（等浮光决策）


## 20:25 Token Secret Scanning 修复

**触发**：push 失败，GitHub secret scanner 拦 MEMORY.md:134 完整 PAT。

**根因**：
- MEMORY.md 自 5 月起含完整 GitHub PAT
- 老 commit `4eb6439` (auto: 5/30 vcp+hermes status) 和 `62a547c` (restore actual token) 也含 token
- 远端 292b505 init commit 是 "token redacted"（`ghp_***REDACTED***`）但 5/30 后老 commit 推不上

**修复**：
- `MEMORY.md:134` 改成缩写 `ghp_ei…r7jz` + 注释"完整 token 在 `~/.openclaw/.secrets/github.pat`"
- 完整 token 存到 `~/.openclaw/.secrets/github.pat` (mode 600)
- `git filter-branch --tree-filter` 改写 4 个含 token 的 commit（master chain 全部）
- `git push --force-with-lease` 推远端
- 本地 `auto-backup-20260530` 分支转 tag `archive/auto-backup-20260530` 保留（5/30 自动备份）

**教训**（已写到 docs/lessons/ 待补）：
- ❌ 永远不在 committed 文件存完整 token
- ✅ 用 `~/.X/.secrets/<name>` (mode 600) 存完整 token
- ✅ MEMORY.md 只存缩写 + 路径
- ✅ amend HEAD 不够时用 filter-branch 改写历史
- ✅ 改写前备份 reflog（已存 `/tmp/reflog-backup-<ts>.txt`）

## 21:06 VCP 完整修复

**修了 4/5 启动 ERROR**：

| ERROR | 修法 | 状态 |
|---|---|---|
| `agent_map.json not found` | `cp agent_map.json.example agent_map.json` | ✅ |
| `ModelRedirect.json 缺失` | `echo '{}' > ModelRedirect.json` | ✅ |
| `rag_params.json 缺失` | `echo '{}' > rag_params.json` | ✅ |
| `EmojiListGenerator/generated_lists/` 缺失 | `mkdir -p Plugin/EmojiListGenerator/generated_lists/`（占位） | ✅ |
| `VexusIndex.recoverFromSqlite is not a function` | **Rust 引擎方法缺失**（`modules/agentManager.js` 用 `VexusIndex` Rust binding，编译错或版本不对）| ❌ 留明天 |

**VCP 架构发现**：
- `Agent/Agent.js` 是 GitHub 404 占位（14 字节"404: Not Found"）—— 不是真文件
- 真实 agent 架构：`modules/agentManager.js`（不是 `Agent/AgentManager.js`）
- 9 个 agent prompt（Nova/Furina/Coco/Ganyu/Hornet/Aemeath/DreamNova/ShaoShenYun/ThemeMaidCoco）以 `.txt` 形式加载 —— 9 个文件全缺失
- AgentManager 启动只加载 mapping，不读文件；调用某 agent 时才报 ENOENT

**VCP 持续跑方案**：
- sandbox exec 启 `node server.js` → exec 结束 node 被 SIGTERM
- 改用 `setsid nohup node server.js` 真正脱离 session
- pid 37201 + 6005 端口 listening

## 21:00 R1 70B tool use bridge（半成品）

**发现**：
- 所有 Q4 量化 model capabilities = `['completion']`，**不支持原生 tools**（Ollama 0.24 验证）
- 唯一原生支持 tools：`llama3.2:1b`（1B 太弱）

**解法**：prompt 注入协议（`<tool_call><name> key="value"</tool_call>`）

**写完**：
- `tools/r1-bridge.py`（8.3 KB）：4 tools (exec/read/write/search) + Ollama chat loop + tool-call parser
- parser 支持 3 种格式：标准 `exec cmd="ls"` / JSON args / qwen `name({...})` 自动推断

**测试结果**（qwen2.5-7b-q4）：
- iter 1-3: model 输出 `name({"cmd": "ls -la"})` 格式（Q4 量化后 qwen 简化 function call）
- parser 已加"自动从 args 推断 tool"（cmd→exec）逻辑
- **未跑完完整测试**（4 次 test 都被 sandbox 切 + heartbeat 切）
- R1 70B (deepseek-r1:70b-q4) **未测**（慢，每个 query ~30s-2min）

**bridge 状态**：脚手架 + parser 全套 ready，明天测 R1 70B。

## 21:08 任务计划更新

按浮光"先修VCP，明天做其他的"——今晚到 VCP 修好为止。

**明天 (2026-06-04) 任务**：
1. **R1 70B 升级**（任务 1）：bridge 跑通 R1 70B + 5 场景 benchmark + 写专有 prompt + 多模型路由
2. **Skill 升级**（任务 2）：Agent-superthinking v2（8 个新思考方法）+ 多语言/多领域模板
3. **VCP 完整修复**：VexusIndex 编译/重装 + 9 个 agent prompt .txt（等浮光给内容）
4. **blogwatcher 安装**（长期挂账）
5. **NLLB torch 重装**（长期挂账）

## 关键文件

- `/home/fuguang/.openclaw/workspace/tools/r1-bridge.py`（8.3 KB）
- `/home/fuguang/VCPToolBox/Plugin/EmojiListGenerator/generated_lists/`（占位）
- `/home/fuguang/VCPToolBox/agent_map.json`（9 个 agent 引用，prompts 缺失）
- `/home/fuguang/VCPToolBox/ModelRedirect.json`（空）
- `/home/fuguang/VCPToolBox/rag_params.json`（空）
- VCP pid 37201
