# 2026-05-17 日志

## 今日主要工作

### 1. Qwen Code 修复
- Coding Plan API Key（sk-sp）过期了
- 切换到通用 API Key `sk-a1b4cc6b7a6643ff823d7c375da5c59a`
- 关键修复：所有 `baseUrl` 从 `coding.dashscope.aliyuncs.com` 改为 `dashscope.aliyuncs.com/compatible-mode/v1`
- 用 `qwen --debug` 验证可用

### 2. YintaTriss 项目调研
扫描了4个项目：

| 项目 | Stars | 结论 |
|------|-------|------|
| AgentSymphony | 1 | 不 adopt，Python 包架构跟 OpenClaw 不兼容 |
| Agent-Superthinking | 2 | Schema 设计值得借鉴（keywords/heuristics/limits），但融合层实现难度高 |
| AgentMemory | 0 | 功能跟 memory-lancedb-pro 重叠，无采纳价值 |
| AgentSearch | 0 | 搜索缓存思路有用，但实现太简单 |

**最有价值的部分：**
- Agent-Superthinking 的索引路由设计
- AgentSearch 的缓存机制

### 3. Skill Registry 创建
路径：`~/.openclaw/workspace/skill-registry.yaml`
- 26个 Skill 注册
- 分层：执行层/搜索层/Agent编排/记忆层/信息层/系统层
- 关键词驱动路由

### 4. 搜索缓存体系
三个文件：
- `tools/search-cache.js` — 通用缓存（Node.js API）
- `tools/search-router.js` — 搜索路由 + 缓存 + fallback
- `skills/openclaw-tavily-search/scripts/tavily_search.py` — 内置缓存的 Tavily 脚本

缓存策略：
- 默认 TTL 1800s（30分钟）
- Tavily 失败自动切换 local-search
- 支持 `--stats` 查看命中率

---

## 明天可以继续

- [ ] 把 Skill Registry 接入 OpenClaw 主 Agent
- [ ] 给 local-search.js 也加缓存层
- [ ] 加 Brave Search 作为第三引擎
- [ ] 测试 search-router 的缓存命中率

---

## 今日教训

- Qwen Code 非 compatible-mode 的 endpoint 会返回 404
- YintaTriss 的项目都是 0 stars，刚发布，不稳定，不能直接用

---

## 资金状态

- Tavily：已配置（tvly-dev-xxx）
- Qwen Code：通用 API Key（sk-a1b4cc6b）
- 本地代理：`http://localhost:18790/search`

---

*最后更新：2026-05-17 21:10*