# 2026-06-04

## 08:38 早晨状态 (V 启动 anchor)

**服务状态**：
- OpenClaw Gateway 18789 ✅ (systemd 拉起，未挂)
- Ollama 11434 ❌ → 拉起 (pid 8157)
- VCP 6005 ❌ → 拉起 (pid 8160, 401=auth 正常)
- AgentTeam Web UI 8080 ❌ → 拉起 (pid 8166)
- Watt pid 7490/7495 ZOMBIE（等浮光 GUI 启用 26561）

**为什么挂**：cron 5d7486d7 没拉起它们（systemd 启 gateway，但 ollama/vcp/agentteam 是 nohup 起的，重启后没自动起来）

**V 端脚本缺**（等浮光决定要不要做）：
- 没有 systemd unit 给 ollama / vcp / agentteam
- 这次是手动 `setsid nohup ... &` 拉的，下次重启还会挂

**10 点闹钟**：
- cron `fb5826d9-5359-4b42-b18e-236087eb1fc0` (deleteAfterRun)
- payload 提示 V 自动保存进度（MEMORY + daily + git push + 列出下次继续点）

## 08:40 P1.1 完成 — AgentTeam 桥接层

### 设计原则（集成不替换）
- V 自己的 model-router.js 保留（单次路由决策）
- AgentTeam 接管"多 agent 协作"（V → bridge → v-core team）
- 浮光通过 AgentTeam Web UI (http://127.0.0.1:8080) 看 v-core 看板

### 改动文件

**新文件**：
- `tools/agentteam-bridge.js` (7.0KB) — V ↔ AgentTeam 桥接
  - `init` — 创建 v-core team（已建）
  - `post-task <subj> [desc]` — V 推 task 到 v-core
  - `update-task <id> <status>` — 改 task 状态
  - `list-tasks [status]` — 列 v-core tasks
  - `recommend <desc> [COMPLEXITY=N]` — V 决策辅助（建议 launch/solo）
  - `status` — 团队状态

**修改文件**：
- `tools/model-router.js` — route() 末尾加 `agentteamHint` 字段
  - 复杂任务（explicitTeam/multiStep/manySubtasks/highComplexity 任一命中）
  - 自动给 `recommend: "dispatch"` + action 命令 + 触发原因
  - 简单任务 → `recommend: "solo"`

**修复 AgentTeam bug**：
- `agentteam/exceptions.py` 加 `ConfigError = ConfigurationError` 别名
- 否则 `agentteam.config` import 失败

### 端到端验证

```
✓ init:        team v-core created (leader: agent id 01652de5521d)
✓ post-task:   c0ac9e7d "P1.1 验证：V ↔ AgentTeam 桥接通"
✓ list-tasks:  1 task pending
✓ recommend 复杂 ("部署 R1 70B ... 并行跑 5 个场景") → dispatch (explicitTeam)
✓ recommend 简单 ("看下今天天气") → solo
✓ model-router 复杂 → agentteamHint.recommend=dispatch
✓ model-router 简单 → agentteamHint.recommend=solo
```

### 噪音问题
- 每次 agentteam CLI 调用刷 4-5 行 WARNING "Config file not found: config.yaml"
- 解决：写个 `~/.agentteam/config.yaml` 默认配置（V 端不动，留给 AgentTeam 维护）

### Web UI 验证
- 浮光现在打开 http://127.0.0.1:8080 应能看 v-core 团队 + 1 task
- 之前 board serve pid 38684，重启后 8166

## 进行中/未做（09:00 前还有时间）
- [ ] P1.2 V cron 5d7486d7 → AgentTeam daemon（统一任务管理）
- [ ] systemd unit 给 ollama/vcp/agentteam（防止下次重启又挂）
- [ ] 09:00 checkpoint - 是否继续推进还是先停

## 10:00 cron 触发后要做（自动）
1. 更新 MEMORY.md 启动 anchor 章节
2. 追加本 daily memory 收尾
3. git commit + push 远端备份
4. 列出下次继续点
