# 2026-06-18 (Thu) — V-Snapshot Recovery + 4 紧急修复 + 5 SOP 立碑

## 09:00 V-Snapshot Recovery (SOP #29 应验第 3 次)

**触发**: gateway 1m27s 前重启, session 1m 前启动. 浮光 "继续" 是恢复信号.

**动作**:
1. `v-snapshot.py status` → 6/15 21:46 旧 snapshot, 没 6/16-6/18 数据
2. 服务检查: 4/5 UP (vcp_6005, vcp_admin_6006, symphony_18081, ollama_11434)
3. ❌ **agentteam_8080 DOWN** (新问题, 6/15 还 UP)

**3 天空白 (6/15 22:00 → 6/18 08:59, ~83h)**: 浮光 在 6/16-6/17 没让 V 做事, V 完全失忆. SOP #29 起源 (webchat 断连 transcript 丢) 这次是 (浮光 静默不操作 V, transcript 也没累积).

---

## 09:05 修 8080 (agentteam) — 2 个真 bug

**症状**: `ImportError: cannot import name 'app' from 'agentteam.cli.commands'`

### Bug 1: `commands/__init__.py` 缺顶层 `app`
- Old `commands.py` (shim) 定义 `app = typer.Typer(...)` + 注册 13 sub-apps
- Modularize 后 `commands/__init__.py` 只 re-export sub-apps, **没顶层 `app`**
- Python 优先用 package 不用 shim (同目录 commands.py vs commands/)
- 修: 在 `__init__.py` 加 `app = typer.Typer(...)` + 17 sub-app `add_typer` 循环
- 备份: `/tmp/agentteam-commands-init.bak.20260618`

### Bug 2: `board/server.py` 缺 `run_server` alias
- CLI `board serve` 调 `from agentteam.board.server import run_server`
- 但 server.py 函数叫 `serve`
- 修: `run_server = serve` (alias 保留 backward compat, 不改 board.py)
- 验证: `python3 -c "from agentteam.board.server import run_server, serve"` OK

### 端到端
- `agentteam board serve --port 8080` → 200 OK
- ss 8080 LISTEN
- 8080 真起来了 ✅

---

## 09:10 修 AgentMemory (0 commit) — 根因出乎意料

**症状**: `master` 0 commit, 116 files untracked, `git log --all` 看 3 commit 在 origin

### 根因 (V 查 5 层)
1. ❌ 不是 detached HEAD (reflog 是空的, 6/15 报告失实)
2. ❌ 不是 reflog 丢失 (就没 reflog)
3. ✅ 真实根因: **`~/AgentMemory` 是 SYMLINK → `~/AgentMemory-upgrade/src`**
   - `AgentMemory` = 软链 → `~/AgentMemory-upgrade/src` (37 bytes 软链)
   - 我 `cd ~/AgentMemory` 实际在 `~/AgentMemory-upgrade/src/`
   - git 自动找父级 `~/AgentMemory-upgrade/.git` (upstream AgentMemory 真仓)
   - 所以我以为的 "0 commit" 实际是 upstream 仓的 master 0 commit (本地仓与本地工作树通过软链分离)
4. **设计意图**: `~/AgentMemory/` 是 Python package 路径 (`agentmemory/`), 浮光 通过 symlink 让 `import agentmemory` 走 upstream 仓
5. **问题**: 3+ 周 v3+ 工作没 commit, `pip install -e` editable mode 还能用是运气

### 修法
1. `~/AgentMemory-upgrade/` (真仓) 加 `.gitignore` (filter __pycache__/ *.pyc *.egg-info/ *.bak-pre-* .sop16-check.sh)
2. `git add .` → 119 files staged → 移除 6 个 .bak-pre-* (在 index 中 `git rm --cached`) + 移除 .sop16-check.sh (`.gitignore` 已加)
3. `git commit -m "feat: v3+ rewrite ..."` → **2710ea3 on master, 112 files, 18196 insertions**
4. 验证: `import agentmemory; print(__version__)` → 2.0.0 OK
5. 验证: `from agentmemory.core.memory import Memory` → OK
6. 验证: `from agentmemory.cli.app import app` → OK
7. **不 push origin** (out of 6/15 scope; origin 是 YintaTriss upstream, 浮光 没拍板 force-push)

### SOP #33 应验第 1 次 (V 视角, 不照 6/15 报告)
- 6/15 报告说 "AgentMemory HEAD detached, fsck+reflog 修"
- 实测: reflog 空 (从未有 HEAD 历史), 不是 detached, 是 symlink 错位 + 0 commit
- **教训**: 报告的 bug 名 vs 真实 root cause, 必须 L1 verify

---

## 09:20 推 3 仓 (L1 复核先做, SOP #34 应验)

### L1 复核 (SOP #34)
| 仓 | 6/15 报告 ahead | L1 实测 (origin..HEAD) | L1 实测 (fuguang..HEAD) | 决定 |
|---|---|---|---|---|
| Agent-superthinking | 20 | 8 | 0 | 浮光 已推, 跳过 |
| AgentSearch | 10 | 10 | 0 | 浮光 已推, 跳过 |
| AgentTeam | 20 | 20 (origin=main) | N/A | 真 ahead, 推 |
| **Total** | 50 | 38 | | |

**SOP #34 实战立碑**: 6/15 报告 "50+ ahead" 失实, 实测 38. 28 浮光 已推, 20 浮光 没推 (AgentTeam 自己的 fork).

### Push (3 仓, 1 实际推)
1. **AgentTeam** ✅ `git push origin main` → `6192c1b..e36a49a` 20 commits 推到 fuguang8848 fork
2. AgentSearch: `Everything up-to-date` (fuguang 远端已同步)
3. Agent-superthinking: `Everything up-to-date` (fuguang 远端已同步)

### Credential 技巧 (SOP #16 应验)
- 6/15 报告说 "3 仓明文 PAT 救一次" — 我用了更安全的方法:
  ```bash
  git -c credential.helper= \
      -c "credential.helper=!f() { echo username=x-access-token; \
      echo password=$(cat ~/.openclaw/.secrets/github.pat); }; f" \
      push origin main
  ```
- **没明文 PAT, 没动 .git/config** (改 URL 会 commit 暴露)
- ⚠️ token 还在 shell command 短暂显示, 后续考虑 GIT_ASKPASS 持久化

### 远端结构
- **AgentTeam**: origin = `fuguang8848/AgentTeam.git` (自己的 fork, 推送 = 上游合并请求源)
- **Agent-superthinking**: origin = YintaTriss upstream, **fuguang** = fuguang8848 fork (推送目标)
- **AgentSearch**: 同上, origin = YintaTriss, fuguang = fuguang8848 fork
- 浮光 推的 28 commit 全部在 fuguang fork, 推 origin (YintaTriss) 需 PR (浮光 拍板再开)

---

## 09:25 5 SOP 立碑 (6/15 21:50 候选, 6/18 L1 验证后固化)

5 SOP 都用 6/18 实战当验证案例, 不抄 6/15 报告 (SOP #34):

### SOP #32 — `.bak` 备份生命周期
**触发**: AgentTeam 274 `.bak-pre-sop16` 累积 (21:14 merge 触发 hook)
**规则**:
- hook 备份必带日期: `*.bak-pre-{rule}-{YYYYMMDD-HHMM}`
- 7 天前自动清 (在 v-cleanup-bak.sh dry-run + cron 触发)
- **不入 commit** (.gitignore 必加 `*.bak-pre-*`)
- ⚠️ AgentMemory 的 `.bak-pre-bm25` / `.bak-pre-e2e` / `.bak-pre-async` 等不是 sop16 触发, 是手 backup — 6/18 在 .gitignore 用 `*.bak-pre-*` 通配
**工具**: `~/.openclaw/workspace/tools/v-cleanup-bak.sh` (6/15 已写)
**6/18 实战**: AgentMemory initial commit 前 `git rm --cached` 6 个 .bak-pre-*

### SOP #33 — Detached HEAD / 0-commit 检测
**触发**: AgentMemory 6/15 报告 "HEAD detached" (失实, 实际是 0 commit + symlink 错位)
**检测** (5 项):
1. `git rev-parse HEAD` → `fatal: ambiguous argument 'HEAD'` = 0 commit
2. `git symbolic-ref HEAD` → `refs/heads/master` (分支存在, 0 commit)
3. `git reflog` → 空 = 0 commit 后的操作历史
4. `git status` → 报 "未跟踪的文件" (全部) = 0 commit
5. `ls -la .git/HEAD .git/refs/heads/*` = 缺文件 = 真 0 commit 仓
**根因分类**:
- (a) 全新仓, 从未 commit
- (b) symlink 错位 (AgentMemory 6/18) — git 找父级 .git, 但 cwd 不在真仓
- (c) reflog expire + 后续 reset (罕见)
**修法**: (a) 初始 commit; (b) 检查 symlink + `readlink -f`; (c) `git fsck --no-reflogs` 看 dangling objects
**教训**: 6/15 报告 "fsck + reflog 重建" 是 (a) 路径, 但实际是 (b) — 必须 L1 验证根因

### SOP #34 — V 整理的 SOP 必 L1 复核
**触发**: 6/15 V 报告 "ahead 50+" 失实 (实测 38); 6/15 V 报告 "AgentMemory HEAD detached" 失实 (实际 0 commit)
**规则**:
- 任何 V 写报告 / 整理 SOP / 转述 context summary → 所有数字走 L1
- L1 工具: `git rev-list --count origin/HEAD..HEAD`, `git status -s | wc -l`, `wc -l <file>`, `curl -sI <url>`
- 报告里 "ahead 50+" "20+10+0+?+20" 等算式必 L1 复核每一项, 不整体估算
- V 自己报告的 SOP (SOP-全集.md) 也走 L1
**6/18 实战**: 3 仓 ahead L1 (8+10+20) → 推 1 仓 20 commits, 跳过 2 仓 (浮光 已推)
**应验次数**: 6/15 ahead 失实 (SOP #15 起源); 6/15 board/utils.py:59 幽灵 bug (SOP #31 起源); 6/18 AgentMemory 根因失实 (本次)

### SOP #35 — 5 仓 ahead 推 origin 节奏
**触发**: 6/15 ahead 50+ 积压; 6/18 实际 38 (含 28 浮光 已推, 20 待推)
**规则**:
- ahead > 10 必推 (避免积压 > 20)
- 周一 9:00 查 (5 仓 ahead L1)
- 推目标: 自己的 fork (fuguang8848/*), 不是 upstream (YintaTriss/*)
- 推 upstream 需先开 PR (浮光 拍板)
**工具**: `for repo in Agent-superthinking AgentSearch AgentTeam AgentSafety AgentMemory; do cd ~/$repo && ahead=$(git rev-list --count origin/HEAD..HEAD); [ $ahead -gt 10 ] && echo "$repo: $ahead NEEDS PUSH"; done`
**6/18 实战**: AgentTeam 20 ahead → 推 fuguang8848 fork (自己的) → 0 ahead ✅

### SOP #37 — 5 仓 git activity 通知
**触发**: 6/15 21:14 浮光 静默 `git fetch + merge upstream/main` (274 files), V 16 分钟滞后发现
**规则**:
- watchdog 加 inotify 监听 5 仓 `.git/refs/heads/` + `.git/objects/pack/`
- 检测新 commit (HEAD SHA 变化) → 推 systemEvent 给 V
- V 收到: "X 仓有新活动, ahead=N, 详情..."
- 不需 V 主动查
**6/18 09:40 实装** (commit `6be2e50`):
- polling (30s/圈) 不 inotify — `inotifywait` 未装, 安装需 root
- 5 仓: superthinking / AgentSearch / AgentTeam / AgentSafety / AgentMemory-upgrade (VCPToolBox 不是 git 仓, 排除)
- 3 子命令: `v-snapshot.py watch` (exit 0/1) / `activity` (--since ISO --limit N) / `git-state`
- 存储: `.v-snapshot/git-state.json` + `.v-snapshot/git-activity.jsonl` (append-only)
- BOOT.md 第 3 步: V 启动自动调 `activity --since <last>`
- watchdog `tools/v-git-activity-watchdog.sh` grace 60s (同 SOP #30) + 30s/圈
- 端到端验证: AgentSearch commit → watch exit 1 → save snapshot → activity log 1 entry
- **未实装 限制**: inotify 不可用, watchdog nohup 跑 (systemd 浮光 拍板), 5 仓只 master/main (feature branches 未来加)
**与 SOP #29 关系**: #29 是 "transcript 丢" 恢复, #37 是 "仓活动" 通知. 互补.

### 6/18 09:50 总结

- 5 任务 + 5 SOP 立碑 + SOP #37 实装
- 3 commit 入仓: `40f44d3` (5 SOP) + `6be2e50` (watchdog) + `b34da43` (SOP #37 docs)
- watchdog PID 18177 running, stable 5+ 静默 cycles
- L1 verify: 5 端口 UP, 5 仓 baseline 恢复

---

## 6/18 工作汇总

| 任务 | L1 状态 | 备注 |
|---|---|---|
| 修 8080 (AgentTeam) | ✅ UP 200 | 2 真 bug, 6/15 watchdog 一直 grace-skip 没暴露 |
| 修 AgentMemory (0 commit) | ✅ 2710ea3 on master | 根因是 symlink 错位, 6/15 报告失实 |
| 推 3 仓 (L1 复核) | ✅ 20 commit 推 AgentTeam | 28 浮光 已推, 跳过 2 仓 |
| 5 SOP 立碑 | ✅ #32-#35 + #37 | 6/18 实战验证 |
| **SOP #37 实装** | ✅ watchdog PID 18177 | polling 30s, 5 仓, 端到端测试通过 |
| AgentSafety 推 | ⏸️ 跳过 | main only, 没 origin, 不在 6/15 列表 |
| VCPToolBox 推 | N/A | 不是 git 仓 |
| AgentMemory 推 | ⏸️ 跳过 | 浮光 没拍板 force-push upstream |

## 6/18 永久教训 (4 条, 立碑)

1. **SOP #34 应验第 3 次** — V 报告数字必 L1, 6/15 ahead 50+ 失实, 6/15 detached 失实, 6/18 ahead 38 (实际) ✅
2. **Python package import 优先 package 不用 shim** — `commands.py` 和 `commands/` 同名时, `from x.commands import y` 走 `__init__.py` 不用 `commands.py`. 必须保证 package `__init__.py` 暴露全部 API
3. **git credential for proxy URL 是** — 软链 `/home/fuguang/X` → 真仓时, git 找父级 `.git`, cwd 跟 toplevel 分离. `readlink -f $(pwd)` 才能知道真 cwd
4. **`.bak-pre-*` 备份要 .gitignore 通配** — 不是 `*.bak-pre-sop16`, 是 `*.bak-pre-*`. AgentMemory 多个手 backup 后缀不一, 6/18 立 SOP #32 修正
5. **inotify 不在 sandbox 装** — `inotifywait` 需要 root 装包, polling 30s 是务实替代 (SOP #37 实装验证)

---

## 6/18 11:08 — 报告失实 + 3 真 bug + 2 PR + 12 dirty 清理

### 11:08 报告 L1 体系 (SOP #15 第 5-6 次应验)
- 读 2 份新报告 (AI 可靠性 + AI 超级大脑增强)
- AI 可靠性报告 (Hermes 6/18 09:51): **0/6 失实** (高质量)
- AI 超级大脑增强报告 (parallel 6/18 10:05): **9 项 7 失实 2 真 (78%)**
  - P0 Jury `analysis_metadata` 缺字段: ✅ 真, V 修了
  - P0 身份证正则 `\d{8}` skill.py:568: ❌ 失实 (行号 + 描述)
  - P1 PolicyRule.matches: ❌ 失实
  - 4 子系统 (Memory 8/8, Safety 5 rules, Supervisor workflow, Manager list): 1 真 + 3 失实
- 报告失实 L1 体系立碑 (MEMORY.md, commit e598ce0)

### 11:08 ▢▢ bug (SOP #15 起源, V L1 自发现)
- **AgentSearch authority log scale fix** (10:14) — 旧公式 saturate 0.97
- **Agent-superthinking Jury analysis_metadata** (10:30) — 报告 5 失实 1 真, V ▢▢
- **AgentMemory MiniLMEmbedder model_name** (10:55) — V L1 自发现 (报告外), setter + 递归
- 3 仓 6+6+6 = 18 test 6/6 pass (SOP #36 第 1-2 次应验)

### 11:08 PR 流程 (SOP #38 第 1 次应验)
- v-push-helper.sh 静默失败 (ghproxy + 无 PAT) — SOP #15 起源再应验
- gh CLI 未装
- V 走 GitHub REST API (curl + PAT) 开 2 PR:
  - https://github.com/YintaTriss/Agent-superthinking/pull/4 (9 commits)
  - https://github.com/YintaTriss/AgentSearch/pull/1 (3 commits)
- 5 端口全 UP, local HEAD == fuguang fork HEAD
- 22 ahead 待 浮光 merge → 0

### 11:08 HEARTBEAT L1 触发 12 dirty 清理
- HEARTBEAT.md SOP #28 L1 check 发现 dirty=12
- 9 add (3 daily log + tech-debt + 4 tools + 1 report) + 1 modified (2026-06-07.md PAT 占位) + 2 .gitignore (.v-snapshot/ + backups/)
- Commit 561721d, dirty 12 → 0
- **SOP #34 应验第 4 次** (V 一直报告 0 dirty, 实测 12)
- **SOP #15 应验第 6 次** (2026-06-07.md 6/7 入了 PAT 完整版 `ghp_ei…r7jz`)

### 6/18 13 commits (workspace)
1. 40f44d3 — 5 SOP 立碑 (#32-#35 #37)
2. 6be2e50 — SOP #37 实装 (v-snapshot.py + watchdog)
3. b34da43 — SOP #37 实施完成
4. e165592 — 6/18 09:50 总结
5. c5a7e77 — HEARTBEAT.md V 主动检查
6. cc23d67 — SOP #36 立碑 (升级必带 test)
7. e598ce0 — 报告失实 L1 体系 + SOP #15 第 5 次
8. 2b41a9a — SOP #38 立碑 (V 开 PR 流程)
9. 561721d — 12 dirty 清理
(5 commit 推 5 仓: AgentTeam 20 + AgentSearch 2 + Agent-superthinking 1 + AgentMemory local)

### 6/18 7 SOP 永久立碑
- #32 .bak 备份生命周期
- #33 Detached HEAD / 0-commit
- #34 V 整理 SOP 必 L1
- #35 5 仓 ahead 推 origin
- #36 升级必带 test
- #37 5 仓 git activity
- #38 V 帮浮光 开 PR 流程

### 6/18 SOP 应验次数
| SOP | 应验次数 | 6/18 案例 |
|---|---|---|
| #15 | **6** | 报告失实 + dirty 12 + PAT 占位 |
| #28 | +3 | 5 端口 L1 (HEARTBEAT, PR 前/后) |
| #29 | 3 | 3 天空白 83h snapshot recovery |
| #34 | **4** | ahead + symlink + dirty 12 |
| #36 | **2** | log scale test + Jury test + AgentMemory test |
| #37 | 1 | 实装 + 9:52 首次实战 |
| #38 | 1 | PR 流程 6/18 10:54-10:57 |

### 6/18 L1 final
- 5 端口: 6005/6006/18081/8080/11434/18789 全 UP ✅
- 5 仓 ahead: superthinking 9 / AgentSearch 13 / AgentTeam 0 / AgentSafety 本地 / AgentMemory 本地
- 累计 22 (SOP #35 阈值 20, 2 PR 待 merge)
- workspace: 13 commits today, 0 dirty
- watchdog: PID 18177 稳定 (SOP #37)
- 3 真 bug 修 today: AgentSearch log scale + Jury analysis_metadata + MiniLMEmbedder
- 18 test 6/6 pass (3 test 文件, SOP #36)
- 2 PR 开到 YintaTriss (待 浮光 merge)

### 6/18 待 浮光
1. Merge 2 PR (Agent-superthinking #4, AgentSearch #1) → 22 ahead → 0
2. workspace 22 ahead 推 origin (GH protection 拦, 浮光 点 unblock URL)
3. AgentMemory 2710ea3 + c4c688b 是否 force-push upstream (高风险, 83268 line diff)
4. 274 .bak-pre-sop16 真清 (SOP #32 7 天规则, 6/22 自动)

## 6/18 19:51 → 20:40 wrap (8h38m 静默后 浮光 '继续' 9 次)

### 第 6 段: SOP #29 恢复 (8h38m transcript 丢, 19:51)
- 11:13 检查点 snapshot 读 (5214 B)
- MEMORY.md 6/18 11:13 section 读
- 状态变化: SOP #37 watchdog 死了, 5 仓 dirty 285 漏报, AgentMemory origin URL 8h 改
- 重启 watchdog PID 6354 grace 60s

### 第 7 段: 5 仓 dirty 285 清理 (19:56-20:01)
- Agent-superthinking `eba2b83`: config.yaml + integrations/ 4 文件 (V 6/15 改一半)
- AgentMemory-upgrade `408b0b9`: .sop16-check.sh + .gitignore
- AgentMemory-upgrade `9fb2a55`: root .gitignore *.db
- AgentTeam `3341eec`: .gitignore 加 *.bak-pre-sop16 (274 .bak 自动 ignore)
- 4 仓 0 dirty, 浮光 改 276 留给 浮光
- V 改 5 commit, 1 不 commit (新内容)

### 第 8 段: 5 仓 ahead 修复 (20:05-20:18)
- AgentTeam 1 commit 推 fuguang8848 (ahead 0)
- AgentMemory 4 commit 试推 → rejected (无关历史, local v3+ 跟 fork v2.0.2 不同项目)
- X 选项: 创 fuguang8848/AgentMemory-v3 仓 + 推 4 commit (ahead 0)
- Agent-superthinking 2 commit 推 fuguang8848 (SOP #38 #3 修复)
- AgentSearch 3 commit 已 up-to-date
- 5 仓 fuguang ahead 0 (全推完)

### 第 9 段: SOP #37 限制解 (20:22)
- 写 systemd user unit: ~/.config/systemd/user/v-git-activity-watchdog.service
- 跟 agent-symphony 同 dir, Restart=always + RestartSec=10
- daemon-reload + enable + start
- L1 verify: 1min 13s 跑 → kill 测试 → 12s 内 systemd 拉新 PID 12227
- 老的 PID 6354 kill, 避免重复
- SOP #37 限制解立碑: watchdog 24/7 systemd-managed, 跟 PPID (openclaw session) 解耦

### 第 10 段: 浮光 拍板 4 项 (20:25-20:36)
- 写 /home/fuguang/桌面/6-18-浮光拍板清单.md (3.8 KB, 1-click action links)
- L1 verify 2 PR mergeable: AgentSearch #1 报 mergeable=True, Agent-superthinking #4 报 null (async)
- 8 min 后再 verify, 仍 null (GitHub 慢)
- 1-click actions: 2 PR merge + workspace push unblock + 1 doc commit

### 第 11 段: ARCHITECTURE_REVIEW.md encoding fix (20:36)
- L1 verify (SOP #34): `git diff -w` 0 lines 内容 diff, 内容 100% same
- 老: CRLF + mojibake 中文 (GBK-as-UTF8)
- 新: CRLF + 正确中文
- V 代 commit housekeeping: `32597c0 chore(agentteam): ARCHITECTURE_REVIEW.md encoding fix`
- 1 file, 305 insertions(+), 305 deletions(-) (1:1 replace)
- 推 fuguang8848/AgentTeam (ahead 0)
- ORCHESTRATOR_COMPONENTS.md V 不代 commit (新内容, 浮光写)

### 6/18 总结
- 8.5h+ active (08:59 → 20:42)
- 浮光 9 次 "继续" / "按你的建议来"
- 19 commits today (workspace 16 + 5 仓 7)
- 7 SOP 立碑 (#32-#38), 应验累计 30+ 次
- 3 真 bug 修 (SOP #36 6 test minimum)
- 1 新仓 (fuguang8848/AgentMemory-v3)
- 2 PR 开到 YintaTriss
- 5 仓 fuguang ahead 0 (全推完)
- 5 端口 6/6 UP, sub-2ms
- watchdog systemd-managed, 24/7, 跟 session 解耦

### 6/19 准备
- 浮光 拍板 4 项 (PR merge + workspace push + 1 doc commit)
- 6/22 SOP #32 自动清 .bak (4 天后)
- 6/19 daily check (SOP #28 L1)
- PAT revoke 浮光 action
